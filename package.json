{
  "name": "ghuilhermecds.github.io",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "postbuild": "touch out/.nojekyll",
    "deploy": "npm run build && npm run postbuild && git add out && git commit -m 'Deploy Next.js site' && git push origin `git subtree split --prefix out main`:gh-pages --force"
  },
  "dependencies": {
    "react": "^18",
    "react-dom": "^18",
    "next": "14.2.3"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18"
  }
}
